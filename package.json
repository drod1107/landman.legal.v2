{
  "name": "landman-legal-frontend",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "parcel build index.html && cp _redirects ./dist/",
    "serve": "parcel index.html"
  },
  "dependencies": {
    "parcel": "^2.10.3"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.12.0",
    "@parcel/transformer-jsonld": "^2.12.0",
    "purgecss": "^5.0.0"
  }
}
