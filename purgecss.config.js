module.exports = {
  content: ['./*.html', './*.js'], // paths to your HTML and JavaScript files at the root
  css: ['./css/**/*.css'], // paths to your CSS files in the /css directory at the root
  output: './css/clean/', // path to output the cleaned CSS files in the /css/clean subdirectory
  // Additional options like whitelisting, extractors, etc.
};
